const { SSEService } = require('../dist/services/sseService'); \n\n/**\n * Direct test of SSE Service stats functionality\n */\nconsole.log('🧪 Testing SSE Service Stats Directly...'); \n\ntry { \n    // Get SSE service instance\n    const sseService = SSEService.getInstance();\n    \n    // Test getStats method\n    const stats = sseService.getStats();\n    \n    console.log('✅ SSE Service getStats() works!');\n    console.log('📊 Stats:', JSON.stringify(stats, null, 2));\n    \n    // Test stats structure\n    if (typeof stats.totalConnections === 'number' &&\n        typeof stats.activeUsers === 'number' &&\n        typeof stats.connectionsByUser === 'object') {\n        console.log('✅ Stats structure is correct');\n    } else {\n        console.log('❌ Stats structure is incorrect');\n        console.log('Expected: { totalConnections: number, activeUsers: number, connectionsByUser: object }');\n    }\n    \n} catch (error) {\n    console.error('❌ Error testing SSE Service:', error);\n    process.exit(1);\n}\n\nconsole.log('🎉 SSE Service stats test completed successfully!'); 